'use strict';

var scrollFunction = function scrollFunction() {
  var scrollPosition = document.body.scrollTop;
  var frontPageHeight = document.getElementById('#splash').style.height;
  if (scrollPosition > frontPageHeight) {}
};

var elem = document.getElementById('#splash').style.height;